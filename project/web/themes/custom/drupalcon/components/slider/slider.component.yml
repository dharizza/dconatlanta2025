name: Slider
props:
  type: object
  properties:
    title:
      type: string
      title: Title
      description: "Title for component"
      examples:
        - My carousel
slots:
  slides:
    title: Slides
    description: Content that overrides the slides in the carousel.
